import { useEffect, useState } from "react";

function CatsPerSecond() {
    const [count, setCount] = useState(0)
    useEffect(() => {
        const catPerSec = setInterval(() => {

        })
    })
}

export default CatsPerSecond
